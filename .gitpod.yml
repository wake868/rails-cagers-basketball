tasks:
  - init: |
      rvm install 3.0.3 --default
      gem install rails
      bundle install
      rails db:migrate
    command: |
      rails server
ports:
  - port: 3000
    onOpen: open-browser
vscode:
  extensions:
    - bung87.rails
    - sianglim.slim
    - bung87.vscode-gemfile
    - alexcvzz.vscode-sqlite
    - rebornix.ruby
